"use strict";
{
    let calcul = {
        plus(a, b) {
            return a + b;
        },
        minus(a, b) {
            return a - b;
        },
    };
    console.log(calcul.plus(1, 2));
    console.log(calcul.minus(1, 2));
}
